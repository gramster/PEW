@BODY
BODY bod_id FOR head_id;

END;
@CHANNEL
CHANNEL ch_id ( r1, r2 );
	BY r1: ;
	BY r2: ;
	BY r1, r2: ;
@OUTPUT
OUTPUT ip.ii(args);
@SPECIFICATION
SPECIFICATION  SYSTEM ;
DEFAULT INDIVIDUAL QUEUE;
TIMESCALE SECONDS;
CONST
TYPE
CHANNEL
IP
MODVAR
VAR
STATE
STATESET
INITIALIZE
TRANS
END.
@STATE
STATE
       s1, s2, s3;
@STATESET
STATESET
      ss = [ s1, s2 ];
@TRANS

